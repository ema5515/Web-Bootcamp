<%- include("partial/header") -%>

<h1>Home</h1>
<p><%= homeStartingContent %></p>

<% posts.forEach(function(post){%>
    <h1 href="/blog/<%= k.kebabCase(post.title) %>"><%= post.title %></h1>
    <p><%= post.content.slice(0,50) + "...";%><a href="/blog/<%= k.kebabCase(post.title) %>">Read more</a></p>
<% }); %>

<% posts.forEach(function(postTitle){%>
    <%console.log(postTitle.title); });%>

<%- include("partial/footer") -%> 